{
	"name": "slopify-frontend",
	"private": true,
	"scripts": {
		"build": "pnpm --color run -r build",
		"dev": "pnpm --color run -r dev",
		"lint-format": "biome check --write"
	},
	"dependencies": {
		"@coreui": "workspace:*"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.5"
	}
}
