# Project Guidelines

- Avoid unnecessary runtime assertions for trusted, application-owned data such as values written to local storage by this application. Prefer direct typed deserialization unless explicit boundary validation is required.
